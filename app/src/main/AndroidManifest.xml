<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.glenn.seng_300_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogTasks"></activity>

        <activity android:name=".Pop"
            android:theme="@style/AppTheme.CustomTheme"
            ></activity>'
        <activity android:name=".NavigationBaseActivity"></activity>
        <activity android:name=".HelpActivity"></activity>
        <activity android:name=".TaskActivity"></activity>
        <activity android:name=".LogTasks" />
        <activity
            android:name=".Pop"
            android:theme="@style/AppTheme.CustomTheme" />
        '
        <activity android:name=".NavigationBaseActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".StartTimeActivity" />
        <activity android:name=".SetTimeActivity"></activity>
    </application>

</manifest>
